# 兵器巡檢記錄

> 讀取時機：進場偵察的「教訓注入」之後，掃描「可萃取精髓」索引。
> 用途：深審過的兵器不一定要裝，但裡面的設計精髓可以在鍛造新工具時被拿出來用。
> 核心邏輯：**記錄是為了未來被觸發，不是為了歸檔。**

---

## 可萃取精髓索引

> 進場偵察時掃描此表。匹配方式：看「鍛造情境觸發詞」是否命中當前鍛造物的需求。
> 命中 → 讀對應兵器的精髓區塊，評估是否適用。

| ID | 精髓 | 鍛造情境觸發詞 | 來源兵器 |
|----|------|---------------|---------|
| E01 | **有序 middleware chain** — 12 層按固定順序插拔，每層只管一件事，before/after 分離 | 流程控制、前處理/後處理、多步驟管線、攔截器、pipeline | DeerFlow |
| E02 | **可插拔 provider 協議** — 用 structural protocol 定義接口，不綁死實作類別 | 需要擴展點、可替換元件、策略模式、pluggable、多種實作 | DeerFlow |
| E03 | **虛擬路徑映射** — agent 看到的路徑 ≠ 真實路徑，輸出自動遮蔽 host 資訊 | 檔案操作、路徑安全、隱藏實體路徑、沙箱、隔離 | DeerFlow |
| E04 | **subagent 委派韁繩** — 並行上限 + 超時 + 工具白名單過濾 + 防遞迴 | 分身、並行任務、子代理、委派執行、背景任務 | DeerFlow |
| E05 | **Harness/App 分層 + CI 邊界守衛** — 核心框架與應用層嚴格分離，測試自動偵測越界 import | 分層架構、核心/外殼分離、防依賴污染、套件化 | DeerFlow |
| E06 | **fail-closed 預設** — 安全機制出錯時阻擋而非放行 | 安全閘門、guardrail、授權、錯誤處理策略 | DeerFlow |
| E07 | **config 熱重載（mtime 偵測）** — 不重啟進程就能吃到新設定 | 設定檔變更、動態配置、不停機更新 | DeerFlow |
| E08 | **debounced queue** — 記憶更新不即時寫入，攢 30 秒批次處理+去重 | 高頻寫入、防抖、批次處理、效能優化 | DeerFlow |
| E09 | **Verify-Loop 翻譯器** — 把祈使句（「修 X」「加 Y」）翻譯成「先寫測試重現 → 失敗證據 → 改 → 測試通過 → 其他測試仍綠」的四拍驗證循環 | 除錯、修繕、修 bug、加功能驗證、test-first、重現問題、verifiable goal | karpathy-skills |
| E10 | **雙份文件反例（⚠️ 反例教材）** — 相同內容寫在 A 和 B 兩個地方（如 CLAUDE.md + SKILL.md 95% 重複）= 無變動地圖 = 改一邊忘另一邊 = 未來必斷裂。誘惑出現時想到這案例，答案永遠是：只寫一個事實源，另一個用引用/symlink 指向它 | 雙份文件、內容重複、單一真相來源、DRY、同步失敗、文件漂移 | karpathy-skills |
| E11 | **改前強制讀全版歷史 → 禁區卡（極簡變體）** — SkillClaw 的 evidence 系統要求改 SKILL 前讀完所有 `history/v*.md`+`v*_evidence.md`（200 字×7 欄位×N 版），對人類讀者太重。火神鍛造簡化為禁區卡：50 字、只記「不能走的路」+ Bridge 指針。核心洞見：**防兜圈子的價值不在記「做了什麼」（git diff 有），在記「為什麼不做另一件事」** | 翻修防兜圈子、修繕歷史追蹤、改了又改回來、SKILL 版本管理、禁區 | SkillClaw |
| E12 | **白名單 sandbox（capability-based security）** — Pydantic Monty 的「from nothing」哲學：啟動時什麼都不暴露，只暴露明確 hand 進去的 functions。比黑名單（Docker / Pyodide）方案更難逃逸——逃逸要找漏洞、白名單沒列就根本沒入口 | sandbox 設計、子 agent 權限、安全隔離、capability-based、最小權限、子代理工具白名單 | skillful-mcp（已安裝） |
| E13 | **Strict mode 配置驗證（DisallowUnknownFields）** — 配置寫錯欄位明確報錯，不靜默跳過。把「typo 在跑時靜默 fallback」這個常見坑變成「啟動時就炸」。比文件級的「請注意拼字」可靠 100 倍 | 配置驗證、spec 寫法、typo 防呆、明確報錯、嚴格模式、配置層 lint | skillful-mcp（已安裝） |
| E14 | **${VAR} 環境變數展開 + missing var 必報錯** — 配置層 recursive walk + missing 必報錯（不靜默 fallback 為空字串）。把 secret 從配置硬編碼搬到環境變數的工法。使用者 2026-04-25 安裝時順手治了 Tavily / Exa key 寫死 .claude.json 的老毛病 | 配置 secret、環境變數、避免硬編碼、機密分離、recursive 替換、配置 secret 治理 | skillful-mcp（已安裝） |
| E15 | **Token-efficient ref-based snapshot** — 不灌整個 DOM 給 LLM，每個可互動元素一個短 ref（e3、e15）。LLM 用 ref 操作（click e15）。比直接灌 accessibility tree 省 90%+ token | LLM 互動工具、page 操作、token 優化、stateless agent、ref-based protocol、結構化頁面表示、號碼牌設計 | playwright-cli（已安裝） |
| E16 | **Packaging 容器與 source-of-truth 分離** — 薄殼 24 行 require 主庫，發布獨立版本。Skill 文件從主庫 `install --skills` 自動生成到 distribution repo | 薄殼設計、distribution layer、source-of-truth、packaging、版本獨立、雙 repo 拓撲、發行隔離 | playwright-cli（已安裝） |
| E17 | **白名單 .npmignore（default-deny 發布）** — `**/*` 全擋 + `!whitelist` 必要檔。未來新加敏感檔不會自動被 publish | 發布套件、ship 工具、default-deny、最小化攻擊面、防誤洩、發行包裝（同 E12 sandbox 精神，套用到 publish 邊界） | playwright-cli（已安裝） |
| E18 | **OIDC npm publish + permissions 最小化** — `permissions: contents:read, id-token:write` + npm OIDC trust，不存 long-lived NPM_TOKEN | CI publish、供應鏈安全、long-lived token 替代、權限最小化、現代 npm 發布姿態 | playwright-cli（已安裝） |
| E19 | **Build-time dogfood validator（Skill 自驗自）** — build script 跑 `validateNoEmDashes` 等 validator 把 SKILL.md 自家規則寫成自動檢查，違規 fail build。比「規則寫進文件等人遵守」強 10 倍，命中「規則已存在 ≠ 規則被遵守」家族 | Skill 自驗、閘門有效性、build-time 校驗、規則自動化、dogfood、自我審計 | impeccable（已安裝） |
| E20 | **絕對禁令清單（match-and-refuse rewrite）** — 6 條樣式禁令寫死在 SKILL.md，遇到就拒絕並重寫。不寫「建議」「最好」，寫「看到必改」。比正向描述「這樣做」強 10 倍 | 禁令清單、絕對禁令、樣式檢查、文本禁令、強制重寫、anti-pattern 列表、match-and-refuse | impeccable（已安裝） |
| E21 | **register 雙 mode 共用 SKILL** — 同一個 SKILL.md 兩種使用模式（brand/product），SKILL 開頭明確切分，每個 sub-command 只在「兩 mode 答案不同時」才寫差異段，相同部分共用。比兩個獨立 SKILL 更省、比合併混在一起更乾淨 | 雙 mode、共用 Skill、模式切換、register、binary 切分、設計選擇樹、SKILL 結構優化 | impeccable（已安裝） |
| E22 | **DESIGN.md 規格（Google Stitch 格式）** — YAML frontmatter 帶機器可讀 design tokens（colors / typography / rounded / spacing / components）+ 6 段固定 markdown（Overview/Colors/Typography/Elevation/Components/Do's and Don'ts）。**Tokens 是規範，prose 是脈絡**。token refs 用 `{path.to.token}` | 設計系統規格、design tokens、Word/簡報設計規範、視覺體系落地、token 真相源、機器可讀設計 spec | impeccable（已安裝） |
| E23 | **雙 sub-agent 不互看獨立評估** — critique 命令啟動兩個 sub-agent 各自獨立做設計評估，**禁互看對方輸出**，最後合併。比一個 head 跑兩遍強——一個 head 跑兩遍會被自己錨定。各跑各的，獨立 tab、獨立 context | 評估、雙盲、防錨定、品質保證、subagent 並行、雙盲評估、獨立評估、honest score | impeccable（已安裝） |

---

## 深審記錄

### DeerFlow (bytedance/deer-flow)

**審計日期**：2026-04-06
**結論**：不安裝。設計精髓萃取為 E01-E08。
**一句話**：LangGraph 上的全棧 SuperAgent——功能最完整的開源 agent 框架，但預設安全配置為開發者體驗優化、非企業安全優化。

**基本資料**：

| 項目 | 值 |
|------|-----|
| Stars | ~57K (+9K/週，GitHub Trending #2) |
| 授權 | MIT |
| 語言 | Python (backend) + Next.js (frontend) |
| 核心依賴 | LangGraph, LangChain |
| 活躍度 | 1,833 commits (2025至今)，20+ 貢獻者 |
| 總分 | 7.5/10 |

**架構摘要**：
- Nginx → LangGraph Server (agent runtime) + Gateway API (REST) + Frontend (Next.js)
- 12 層 middleware chain：ThreadData → Uploads → Sandbox → DanglingToolCall → Guardrail → Summarization → TodoList → Title → Memory → ViewImage → SubagentLimit → Clarification
- Sandbox 雙模式：LocalSandbox（零隔離，開發用）/ AioSandbox（Docker 容器隔離）
- Subagent 系統：最多 3 並行、15min 超時、工具白名單
- 記憶系統：LLM fact extraction → debounced queue → JSON 持久化
- Guardrail：可插拔 provider、fail-closed、但預設關閉

**安全重點（決定不裝的主因）**：
- API 零認證 + CORS `*` = 非 localhost 即裸奔
- LocalSandbox 名為沙箱實為直接執行
- Bash 路徑驗證是 regex best-effort，shell 展開可繞
- Docker 映像預設從北京 Volcengine 拉取
- 前端 rehype-raw 無 DOM 消毒
- Docker socket 掛載 = 容器內可控 host

**正面觀察**：
- 無隱藏遙測/回傳（全面掃描確認）
- CLAUDE.md 556 行，是極優秀的 AI-readable 架構文件
- Harness/App 分層有 CI 測試守護
- 90+ 測試檔案

---

### karpathy-skills (forrestchang/andrej-karpathy-skills)

**審計日期**：2026-04-14
**結論**：不安裝。設計精髓萃取為 E09（Verify-Loop）+ E10（雙份文件反例）。另外兩條通用型精髓已升級到鍛造標準（Tradeoff 宣告、簡版附升級信號、反例並排寫作格式）。
**一句話**：Karpathy 一條 tweet 引爆的 27K-star 迷你 repo——四條 LLM 寫程式紀律原則，內容濃縮漂亮但作者兩個月零維護。

**基本資料**：

| 項目 | 值 |
|------|-----|
| Stars | 27,772（11 週齡，~2,500/週病毒擴散） |
| Fork/Star | 0.082（健康下緣） |
| 授權 | README 宣稱 MIT，但 repo 無 LICENSE 檔（Issue #42）|
| 語言 | 純 Markdown + 2 個 JSON config |
| 活躍度 | 22 commits 總數，2/16 後兩個月零實質開發，4/13 唯一 commit 是加作者自家 Multica 專案連結 |
| 規模 | 857 行（CLAUDE.md 66 + EXAMPLES.md 522 + README 163 + SKILL.md 67 + 2 json）|
| 總分 | 62/100（觀望） |

**L0 安全掃描**：🟢 CLEAN
- 無 .gitattributes filter、無子模組
- CLAUDE.md 存在但內容為純行為指引
- 無零寬字元、無指令覆寫模式、無 build script、無二進位、無 Secret
- 唯一 curl 指令指向自家 raw.githubusercontent.com（安裝用）

**精髓萃取明細**：
- **E09 Verify-Loop 翻譯器** — 情境型精髓（除錯/修繕類 Skill 鍛造時觸發）
- **E10 雙份文件反例** — 反例教材（`CLAUDE.md` 66 行 + `SKILL.md` 67 行，內容 95% 重複，無變動地圖）
- **升級到鍛造標準**（通用型不放巡檢記錄）：
  - Tradeoff 宣告 → 完整 Skill 結構 checklist
  - 簡版附升級信號 → WHAT 層施工規範第 6 條
  - 反例並排寫作格式 → 獨立小節

**不裝的主因**：
- 內容與 使用者 《開發憲法》重疊 70%（測試優先、證據先於宣稱等已有）
- 作者維護訊號差（兩個月零開發 + 最近 commit 是自家廣告）
- context 膨脹成本 > 增量價值

---

### SkillClaw (AMAP-ML/SkillClaw)

**審計日期**：2026-04-16
**結論**：不安裝。設計精髓萃取為 E11（禁區卡前身）。
**一句話**：N users 共同演化 SKILL.md 的開源框架——Local proxy 攔截 agent 對話 → Evolve server 用 LLM pipeline 或 meta-agent 自動改 SKILL.md → 共享 OSS/S3 storage。概念命中「Skill 比模型活得久」但工程成熟度不足。

**基本資料**：

| 項目 | 值 |
|------|-----|
| Stars | 542（5 天齡，~108/day 病毒級起飛但基底極淺） |
| Fork/Star | 0.114（健康下緣） |
| 授權 | MIT |
| 語言 | Python 98.9% |
| 活躍度 | 2 commits / 1 contributor / 0 releases / 0 tests / 0 CI |
| 總分 | 45/100（觀望） |

**L0 安全掃描**：🟡 MEDIUM
- 無 .gitattributes filter、無子模組
- EVOLVE_AGENTS.md 存在（meta-agent 操作手冊，內容正常但揭示架構會自動改本地 SKILL.md）
- 無 eval+decode、無 shell=True、無 hardcoded secret
- ⚠️ proxy_host 預設 0.0.0.0（全網路可見）
- ⚠️ 預設 OSS endpoint 指向阿里雲杭州區 + LLM 預設 DashScope

**不裝的主因**：
- 2 commits / 1 contributor / 0 tests — 論文 demo 成熟度
- LICENSE 作者（北美學者 PI 群）≠ paper 作者（1DreamX 阿里工程師）≠ commit 作者（Yux1angJi）— 治理結構不清
- 演化哲學（統計收斂）跟火神鍛造（精準鍛造）世界觀衝突
- Claude Code 支援沒承諾（Issue #5 未回應）

**精髓萃取明細**：
- **E11 禁區卡** — SkillClaw 的 evidence 200 字 × 7 欄位太重，啟發設計 50 字禁區卡。核心洞見：防兜圈子的價值在記「為什麼不做另一件事」

---

### skillful-mcp (kurtisvg/skillful-mcp)

**審計日期**：2026-04-25
**結論**：✅ **已安裝**。設計精髓萃取為 E12-E14（已安裝，可實機參照）。
**一句話**：把 N 個 MCP server 抽象成 4 個 progressive disclosure 工具的 Go proxy——解 MCP 圈 2026 第一痛點（200+ tools 撐爆 system prompt）。

**基本資料**：

| 項目 | 值 |
|------|-----|
| Stars | 58（11 天 0→58 = 5.3/day） |
| Fork/Star | 4/58 = 0.069（健康下緣以下） |
| 授權 | MIT |
| 語言 | Go 99.7% |
| 活躍度 | 37 commits，1.5 個人類 contributor（kurtisvg 主力 34 + evalstate 1 commit day-0），Renovate bot 自動 dep update。04-13 後 12 天無實質開發 |
| 規模 | ~2000 行 Go（含 test）；9 層架構，36 節點 / 74 邊（understand-anything 圖譜跑過驗證乾淨）|
| 總分 | 66/100（觀望——設計品味高，維護動量需等待重啟） |

**L0 安全掃描**：🟢 CLEAN
- L-1 安全著陸：無 .gitattributes filter、無子模組
- 閘門 A 憲法防護：無 AI 設定檔、無零寬字元、無指令覆寫、無 SVG 腳本
- 閘門 B 供應鏈：直接依賴 3 個（gomonty + mcp go-sdk + pflag），全知名；無 Secret；MIT；distroless nonroot Dockerfile
- 閘門 C 惡意碼：無 eval+decode、無動態外部 import、CI workflow 用 tag 非 SHA pin（🟡 MEDIUM 但全官方 action）

**核心設計（4 個 tools 一手包）**：
- `list_skills`：index（每 skill 一行描述）
- `use_skill(name)`：schema on-demand（取得指定 skill 的 Python signature）
- `read_resource(skill, uri)`：thin proxy 讀 resource
- `execute_code(code)`：在 pydantic/monty Rust sandbox 跑 Python，downstream tools 都暴露為函式

**使用者 個人化配置（最終版）**：
- Binary：`<skillful-mcp binary>`（v0.1.0 windows_amd64，checksum verified）
- Config：`<skillful-mcp config>`（**6 個 server**：notebooklm / tavily / exa / chrome-devtools / playwright / kkh-slide-viewer。paper-search 移除——見下方「Windows 必踩雷 #3」）
- Token：寫在 `.claude.json` 的 skillful entry env 段（不靠 OS 環境變數——見下方「Windows 必踩雷 #2」）
- setx HKCU\Environment 仍保留（TAVILY_API_KEY / EXA_API_KEY / PAPER_SEARCH_EMAIL）作為長期備援，將來 Explorer 重啟後自然 pickup
- .claude.json 備份：`~/.claude.json.before-skillful-20260425-221154`
- Project-level playwright fallback 保留（萬一 skillful 出問題）

**反向掃描 + 不能斷鏈處理**：
- 法眼 SKILL.md / 深度研究 _index.md / 火神鍛造 SKILL.md 加了 progressive disclosure 提醒段（2026-04-25 起）
- nba-ceo / nba-fal / nba-playoff-ceo / 占星師 / Google工具箱 / 字幕翻譯 / 文本參謀長 / 簡報大師 / 表格地下司令 / 視覺導演 / 顧問團 → **無 MCP 引用，不需動**

**裝的主因**：
- 治 200+ tools 撐爆 system prompt（預估省 95%+ tool schema token）
- Monty sandbox 強度高（Pydantic 出品的 Rust capability-based sandbox，6,982 stars）
- 順手治 hardcoded secret 老毛病（${VAR} 抽出 → 環境變數）

**精髓萃取明細**：
- **E12 白名單 sandbox** — 通用型，未來鍛造子 agent 權限時可借鑑「from nothing」哲學
- **E13 Strict mode 配置驗證** — 通用型，未來寫 Skill spec / config 時可借鑑「DisallowUnknownFields」
- **E14 ${VAR} 環境變數展開** — 通用型，配置 secret 治理可借鑑

**🔴 Windows 安裝必踩三雷**（2026-04-25 安裝時逐一踩過、逐一修復；未來重裝 / 換機器時直接照修）：

| # | 症狀 | 根因 | 修法 |
|---|---|---|---|
| **#1** | 所有 STDIO server connect 失敗：`calling "initialize": EOF` | skillful-mcp 的 `internal/mcpserver/server.go` 行 150-156 `toEnv()` **強制傳 empty slice 不繼承父程序 env**。Linux 下 OK，Windows 下子程序沒 PATH/SystemRoot 找不到 cmd/uvx/npx，立刻 exit | mcp.json 每個 STDIO server 的 env 段補：`PATH` / `SystemRoot` / `TEMP` / `TMP` / `USERPROFILE` / `APPDATA` / `LOCALAPPDATA` 全用 `${VAR}` 引用（skillful-mcp 主程序展開） |
| **#2** | `failed to load config: TAVILY_API_KEY is not set` 即使 setx 已成功 + Claude Code 重啟 | Windows setx 寫到 HKCU\Environment registry，**只有從 Explorer 重新啟動的 process 才會讀取**。Explorer 自己也要重啟或登出登入。「重啟 Claude Code」不夠 | 治本：登出登入 Windows / 重啟 Explorer.exe ｜ 立即可用：把 token 直接寫到 `.claude.json` 的 skillful entry **env 段**（Claude Code spawn skillful-mcp 子程序時直接 pass，繞過 OS env） |
| **#3** | `connecting to "paper-search": calling "initialize": EOF` | 套件本身：`Package paper-search-mcp does not provide any executables`。**不是 skillful-mcp 引入的**——原 .claude.json 配置就壞，但 Claude Code 容忍 server spawn 失敗所以 使用者 沒注意。skillful-mcp 嚴格性把它揭露了 | 暫時拿掉。未來找正確 entry point（可能 `uvx --from paper-search-mcp <command>`）或換套件名 |

**附帶啟示**（Skill 設計層）：
- skillful-mcp **「一個 server 失敗整個 abort」嚴格性**比 Claude Code 「容忍 spawn 失敗」更早暴露問題——這是優點不是缺點，但要心理準備
- toEnv 嚴格隔離精髓（E12 白名單 sandbox）在 Windows 是「正確設計遇到平台現實」的典型案例：精髓可借鑑，照搬到 Windows 必爆

**驗證結果**（2026-04-25）：
- ✅ 測試 1（list_skills 6 個 skill 列出）通過
- ✅ 測試 2（法眼 NotebookLM 認證探測 + 走 progressive disclosure 流程）通過
- 測試 3（深度研究 Tavily/Exa）未跑（前兩個已充分證明因果鏈完整）

---

### playwright-cli (microsoft/playwright-cli)

**審計日期**：2026-04-26
**結論**：✅ **已安裝**。設計精髓萃取為 E15-E18（已安裝，可實機參照）。
**一句話**：Microsoft 官方薄殼 CLI——把 Playwright 瀏覽器自動化能力包成命令列工具，給 coding agent 走「token-efficient ref-based」路徑取代或補充 MCP 模式。在 skillful-mcp 配置的 playwright MCP 之外多開一條 stateless 一次性管線。

**基本資料**：

| 項目 | 值 |
|------|-----|
| Stars | 9.4k（93 天齡，~700/週、~100/天，從舊 repo 重啟編號） |
| Fork/Star | 473/9400 = 0.05（書籤型，正常——這不是要 fork 改的工具） |
| 授權 | Apache-2.0 |
| 語言 | JS/TS（薄殼，僅 88 行）+ Markdown（2475 行 skill 文件，28× 於程式碼） |
| 核心依賴 | `playwright`（Microsoft 官方主庫，唯一直接依賴）|
| 活躍度 | 73 commits / 9 contributors / 10 releases (v0.1.0-v0.1.9) / 2026-04-24 v0.1.9 釋出（深審前 2 天） |
| 治理 | Pavel Feldman（Playwright 創辦人之一，前 Puppeteer）46/73 commits = 63%；Yury Semikhatsky 12 commits | 
| 總分 | 78/100 |

**L0 安全掃描**：🟢 CLEAN（單一 🟡 MEDIUM）
- L-1 安全著陸：無 .gitattributes filter、無子模組
- 閘門 A 憲法防護：CLAUDE.md（30 行純 commit convention）+ `.claude/skills/dev`（dev workflow，給開發本 repo 的人）+ `skills/playwright-cli/`（安裝目標 SKILL）內容皆乾淨；零寬字元/RTL/HTML 注入/SVG 腳本掃描全 0 hit
- 閘門 B 供應鏈：直接依賴僅 1 個（playwright），無 install hook、無 build script、無二進位、無 Secret；`.npmignore` 用白名單模式
- 閘門 C 惡意碼：無 eval+decode、無動態外部 import、無可疑 IP/域名
- 🟡 MEDIUM：CI workflows 用 GitHub 第一方 actions 但 tag pin 非 SHA pin（但 publish.yml 已用 OIDC 取代 NPM_TOKEN，現代最佳實踐）

**核心設計**：
- `playwright-cli.js`（24 行）= 純薄殼，require `playwright-core/lib/tools/cli-client/program`，把 npm version 傳進去
- `scripts/update.js`（42 行）= 跑 `playwright-cli install --skills` 從主庫自動生成 skill 文件，搬到 `skills/playwright-cli/`
- 真實 CLI 邏輯在 `microsoft/playwright/packages/playwright/src/mcp/terminal`（CONTRIBUTING.md 第一行明寫——所以這個 repo 的「架構審查」等於信不信主庫）
- ref-based snapshot（E15 精髓）：每個可互動元素一個短 ref（e3、e15），LLM 用 ref 操作

**使用者 安裝記錄**（2026-04-26）：
- 全域裝：`npm install -g @playwright/cli@latest`（npm prefix `<npm prefix>`，已在 PATH）
- 版本：v0.1.9，3 個 npm package 共 3 秒裝完
- Skill 鋪到：`~/.claude/skills/playwright-cli/`（在 `~/` 跑 `playwright-cli install --skills`）→ Claude Code 已識別為 `playwright-cli` Skill
- Browser：自動偵測到系統 Chrome（無需另載 chromium binary）
- 副產品：`~/AppData/Local/ms-playwright/ffmpeg-1011`（video-recording 用，1.3MB）+ `winldd-1007`（0.1MB），都是 Playwright 官方依賴

**驗證結果**（2026-04-26 即裝即測）：
- ✅ 測試 1（`playwright-cli --version` 回 0.1.9）通過
- ✅ 測試 2（todomvc demo：open + snapshot 拿 ref `e8` textbox + type "Buy groceries" + Enter + 第二條 todo + snapshot 看到加進去）通過
- ✅ 測試 3（`eval "[...document.querySelectorAll('.todo-list li label')].map(el => el.innerText)"` 一行抓出 `["Buy groceries", "Test playwright-cli with 使用者"]`）通過 — **核心精髓 E15 ref-based 互動 + JS eval 結構化抓取雙確認**
- ✅ 測試 4（`screenshot --filename=todo-test.png` 存到 /tmp，37KB PNG）通過
- ✅ 測試 5（`close-all` 清乾淨，無殘留 browser 進程）通過

**裝的主因**：
- 使用者 拍板：「光是讀網頁 CLI 比 MCP 好」——一次性 stateless 抓取場景 CLI 開銷更低（無 schema 灌 system prompt、無長駐 server）
- 開發憲法第六條（善用瀏覽器工具做 E2E 測試）— 直接命中目標
- 補 platform-search-playbook 工具鏈缺口：JS 渲染後 DOM、需登入內容（state-save/state-load）、視覺證據（screenshot/tracing）三類場景
- 與 skillful-mcp 內 playwright MCP entry 並存：**CLI 跑 stateless 一次性、MCP 跑 stateful 長 session**——兩者不是競爭，是不同任務的最佳工具

**精髓萃取明細**：
- **E15 Token-efficient ref-based snapshot** — 通用型，未來鍛造 LLM 互動工具時可借鑑「給號碼牌不給整張頁面」
- **E16 Packaging 容器分離** — 通用型，未來設計 distribution-only repo 時的拓撲（content 在主庫、wrapper 獨立 versioning）
- **E17 白名單 .npmignore** — 通用型，發布工具時的 default-deny 姿態（同 E12 sandbox 精神，套用到 publish 邊界）
- **E18 OIDC npm publish** — 通用型，CI 供應鏈安全現代姿態（取代 long-lived NPM_TOKEN）

**附帶啟示**（深審層 meta 觀察）：
- 「CLI vs MCP 不是競爭關係，是不同任務的最佳工具」這個判斷是 使用者 在審計過程中拍板的，比工頭原本「功能重疊 → 不裝」的推薦更精準（命中 `feedback_user_hypothesis_before_system_analysis.md`）
- 對「程式碼極少（88 行）+ markdown 主導」的 packaging 容器，understand-anything 圖譜跑出來資訊量低 — L4 跳過並說明比強跑五個 phase subagent 更尊重時間（命中工頭世界觀「規則是死的，工頭是活的」+ YAGNI）
- 對「永遠 X > Y」這種斷言要敢挑戰——不是順從使用者所有觀點，而是順從**經得起反例的觀點**（命中 `feedback_principle_scope.md`）

---

### impeccable (pbakaus/impeccable)

**審計日期**：2026-04-30
**結論**：✅ **已安裝**。設計精髓萃取為 E19-E23（已安裝，可實機參照）。
**一句話**：給 AI 寫前端 UI 時用的「設計品味調教器」——前 Google DevRel / jQuery UI 創辦人 Paul Bakaus 出品，1 個 Skill / 23 commands / 35 reference / 11 種 AI 工具支援，治 LLM 「Inter 字體 + 紫色漸層 + 卡片堆卡片 + 灰字白底」AI slop 反射通病。

**基本資料**：

| 項目 | 值 |
|------|-----|
| Stars | 23,222（165 天齡，~140/day 病毒級） |
| Fork/Star | 1173/23222 = 0.05（書籤型，工具類正常） |
| 授權 | Apache 2.0 |
| 語言 | JavaScript 89.1% + Markdown 主導內容 |
| 核心依賴 | jsdom 29.0.0 + marked 16.4.2 + 可選 puppeteer |
| 活躍度 | 630 commits / 5.5 月，過去 30 天 341 commits = ~11/day（極活躍）；最後 commit 2026-04-28；Skill v3.0.5 於 2026-04-29 釋出 |
| 治理 | Paul Bakaus 主力 + 數人 PR；CODEOWNERS 全 @pbakaus |
| 規模 | 35 reference markdown 共 6246 行 + 16 scripts + 17 test 檔（unit/e2e/jsdom/framework-fixtures）+ 29 條 antipattern detection 規則 |
| 總分 | 87/100（🔥 驚豔） |

**L0 安全掃描**：🟢 CLEAN（兩個 MEDIUM 觀察）
- L-1 安全著陸：無 .gitattributes filter、無 submodule
- 閘門 A 憲法防護：CLAUDE.md / AGENTS.md 純粹合法開發指引；零指令覆寫；1 個零寬字元 hit 確認為 JSX comment syntax 設計（合法）
- 🟡 MEDIUM：CLAUDE.md / AGENTS.md 提到的 SKILL.md 開頭 `<post-update-cleanup>` block **實際不存在**（v3.0.5 應已移除自動執行機制，但兩個 instructions 文件未跟著更新——文件 drift）
- 閘門 B 供應鏈：Apache 2.0 / 0 secret / 0 install hook / 0 異常二進位
- 🟡 MEDIUM：`skill-review.yml` 用 `tesslio/skill-review@main`（branch ref 而非 SHA pin）
- 閘門 C 惡意碼：0 eval+decode / 0 動態外部 import / 0 可疑外連

**使用者 個人化配置**：
- Skill：`~/.claude/skills/impeccable/`（35 reference + 15 scripts + SKILL.md，828K）
- CLI：`~/AppData/Roaming/npm/impeccable`（v2.1.8，全域）
- 安裝路徑：從 clone `cp -r .claude/skills/impeccable` → `~/.claude/skills/`（取 v3.0.5 已 commit build output）+ `npm i -g impeccable` 拉 CLI binary
- ~/.claude.json 沒額外設定（impeccable 走 default mechanism）

**反向掃描**：
- 0 命令名衝突（既有 18 個 skill 中無任何 SKILL.md frontmatter name 命中 impeccable 的 23 個 sub-command）
- **不要 pin standalone shortcut**——`/audit` `/polish` `/critique` 等通用詞會跟未來 Skill 衝突，全留在 `/impeccable` namespace 下

**驗證結果**（2026-04-30）：
- ✅ 測試 1（CLI `impeccable --version` 回 2.1.8）通過
- ✅ 測試 2（5 種 antipattern 故意 HTML smoke test：`border-left:4px + border-radius:12px` / `font-family: Inter` / `color: #888` on `#fff` / 紫色 / `background-clip: text` + gradient → CLI 抓出 5/5 [side-tab / low-contrast / overused-font / ai-color-palette / gradient-text]，exit code 2，JSON 結構良好）通過
- 測試 3（在 Claude Code session 內呼叫 `/impeccable` 跑 sub-command）未跑——本對話 session 不重啟跑不到，下次新對話自然驗證

**裝的主因**（使用者 拍板 Q1=C / Q3=A）：
- 賽事分析 交付物想改成網頁呈現（好看 + 不易壞，Excel 太脆）
- 未來潛在做網頁需求（賽事分析 儀表板 v2 / 個人作品集 / 對外服務頁）
- 安裝下去後，其他 Skill 借鑑時有完整檔案可參考——本身就是「設計嚴謹度」教科書

**精髓萃取明細**：
- **E19 Build-time dogfood validator** — 通用型，火神鍛造可借鑑寫自驗腳本（自掃踩雷錄同類記憶 ≥3 = 升級閘門 / 自掃 SKILL.md 違反自家飛輪原則）
- **E20 絕對禁令清單** — 通用型，賽事分析 `_WRITE_SPEC` / 文本參謀長 / 占星師 都可寫專屬絕對禁令（match-and-refuse rewrite）
- **E21 register 雙 mode** — 通用型，占星師（合盤 vs 個人深度）/ 賽事分析（例行賽 vs 季後賽）/ 火神鍛造（外部客戶 vs 火神自身）都已是雙 mode 結構，可借鑑強化結構切分
- **E22 DESIGN.md Stitch 規格** — 通用型，Word（文本參謀長）/ 賽事分析 Sheet 1 / 占星師報告 都可寫專屬 DESIGN.md 把散落字面決策落地成 token 真相源
- **E23 雙 sub-agent 不互看獨立評估** — 通用型，火神驗收 / 顧問團 / 淬煉團 都可借鑑「雙盲評估」工法（並行 sub-agent + 禁互看 + 合併）

**附帶啟示**（深審層 meta 觀察）：
- 工頭第一輪推薦「不裝」過於草率——使用者 連續追問 + 場景延伸（「web/word/其他設計場景，它只能做網頁嗎」）揭露邊界並非「frontend-only」（命中 `feedback_user_questions_are_quality_gates.md` 第 N+? 次 helpful）
- impeccable 的 5 層架構（detection / live / 禁令 / commands / 知識庫）邊界不一，**只把它當「Web frontend Skill」會錯過 60-70% 通用設計知識**——要拆層看
- 「這把斧頭很鋒利但你工地裡沒有要砍的樹」這個結論被 使用者「我未來會種樹」否決——**長期視角的決策不能用當下需求否決**

---

## 運作規則

1. **新增記錄**：每次深審結束，提取可萃取精髓寫入索引 + 記錄區塊
2. **觸發詞設計**：每個精髓必須寫「鍛造情境觸發詞」——用鍛造時會自然出現的詞彙，不是兵器本身的術語
3. **進場掃描**：鍛造新工具時，進場偵察掃描精髓索引，看有沒有命中的設計模式可借鑑
4. **引用回標**：實際借鑑某精髓後，在精髓索引加標記（用過 N 次、效果如何）
5. **容量控制**：精髓索引 ≤30 條。超過就濃縮——相似的合併，從未被觸發的檢討觸發詞或移除
